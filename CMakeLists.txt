# Top-level ESP-IDF project file for the ESP32-S3-EYE BirdNET firmware.
# Requires ESP-IDF >= 5.4 (pulled in by the espressif/esp32_s3_eye BSP).
cmake_minimum_required(VERSION 3.16)

include($ENV{IDF_PATH}/tools/cmake/project.cmake)

project(esp32_s3_eye_birdnet)
